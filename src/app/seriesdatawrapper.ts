import { Series } from './series';
import { SeriesAlias } from './seriesalias';
import { SeriesSummary } from './seriessummary';

export class SeriesDataWrapper {
  series: Series;
  seriesSummary: SeriesSummary;
  seriesAlias: SeriesAlias;
}